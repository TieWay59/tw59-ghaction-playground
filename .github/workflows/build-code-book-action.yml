name: build code book

on:
  workflow_dispatch:
  push:

jobs:
  convert:
    if: ${{ !startsWith(github.ref, 'refs/tags/') }}
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2 # access repo files

      - name: PandocLatex
        uses: ./
        with:
          args: >-
              pandoc assets/test-1.md 
              -o artifact/build.pdf
              --from markdown 
              --template eisvogel.latex 
              --shift-heading-level-by=-1 
              --resource-path=assets 
              --number-sections 
              --highlight-style monochrome 
              --top-level-division=chapter 
              --pdf-engine "xelatex" 
              --toc 
              -V CJKmainfont="Noto Serif CJK SC" 
              -V toc-own-page="true" 
              -V book 
              -V titlepage="true" 
              -V titlepage-color="FFFFFF" 
              -V code-block-font-size="\normalsize" 
              -V titlepage-rule-height="0"

      - name: UploadArtifact
        uses: actions/upload-artifact@master
        with:
          name: build-${{ github.run_number }}
          path: artifact

      - name: PushCommit
        if: ${{ !startsWith(github.ref, 'refs/tags/') }}
        run: |
          cp artifact/build.pdf backup/build-${{ github.run_number }}.pdf
          git config user.name TieWay59
          git config user.email tieway59@foxmail.com
          git add .
          git commit -m "CI: produce build-${{ github.run_number }}.pdf"
          git push
  release:
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-18.04
    steps:
      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifact/build.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
